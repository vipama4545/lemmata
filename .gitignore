# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment
.env
.env.*
!.env.example
!.env.production.example

# Tooling / agent config
.claude
.cache
*.tsbuildinfo

# Local operational notes — deploy runbook, registry names, host specifics
*.local.md

# Local data-build scripts
scripts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
# Project-wide editor settings, not personal ones — currently just which Tailwind lint rules
# this codebase wants off. Tracked so everybody's editor agrees.
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Database dumps — carry user emails, session tokens and study progress
backups/
*.dump
*.sql.gz
